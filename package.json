{
  "name": "wdywfm",
  "version": "1.2.0",
  "description": "Straightforward network request analysis with session tracking and bot detection detection",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch --mode development",
    "build": "vite build",
    "preview": "vite preview",
    "package": "./scripts/package-extension.sh",
    "preversion": "npm run build",
    "version": "node scripts/sync-version.js && git add manifest.json",
    "postversion": "npm run package && git push origin $(git rev-parse --abbrev-ref HEAD) --tags"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "@vitejs/plugin-vue": "^5.0.0",
    "typescript": "^5.3.3",
    "vite": "^5.0.11",
    "vue": "^3.4.15"
  },
  "dependencies": {
    "@material-icons/svg": "^1.0.33",
    "vue-cocoon": "^1.0.1"
  }
}
