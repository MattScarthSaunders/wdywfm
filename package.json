{
  "name": "network-analysis-extension",
  "version": "1.0.0",
  "description": "Advanced network request analysis with session tracking and bot detection",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch --mode development",
    "build": "vite build && npm run copy-assets",
    "copy-assets": "node -e \"const fs = require('fs'); if (fs.existsSync('icons')) { fs.cpSync('icons', 'dist/icons', { recursive: true }); }\"",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "@vitejs/plugin-vue": "^5.0.0",
    "typescript": "^5.3.3",
    "vite": "^5.0.11",
    "vue": "^3.4.15"
  }
}

